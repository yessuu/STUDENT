import { BooleanIconsDirective } from './boolean-icons.directive';

describe('BooleanIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new BooleanIconsDirective();
    expect(directive).toBeTruthy();
  });
});
